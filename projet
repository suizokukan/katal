Python3

doc: the extension stored in SELECT does not begin with a dot.

    todo :
    $$ --generate_configfile
    $$ todo
    $$ tests, en particulier sous Windows et sous OSX
    $$ doc (projet, README.md)
    $$ pylint

point fort du programme : monofichier + pas de dépendance externe

#
# un principe : on ne touche pas au répertoire de départ.
#

# -h --help
# --version
# --configfile
# --select / --add (=select+add)
# --infos        (liste des extensions, ..., taille cumulée, nombre de fichiers)
# --quiet

# prend un répertoire plein de nombreux répertoires avec des fichiers...
#       crée un répertoire avec les fichiers renommés (sans s'occuper des 
#

# exclure tout répertoire qui...
# exclure tout fichier qui...

# o liste de tous les fichiers dans un répertoire avec différents critères (nom du fichier/taille)
# o catalogue de ces fichiers numérotés :       0 -> dir/subdir/dfs.jpg
# o messages dirigés vers un fichier de log.
# liste des fichiers renumérotés / détection des doublons / élimination des fichiers selon taille,name(regex)


doc : SELECT:# hashid : complete filename, source_path, filename without extension, extension, size, date